package ke.co.skyworld.repository.beans;

public class SQLEnums {
    public enum OrderBy {
        ASC,
        DESC
    }

    public enum Aggregate {
        DISTINCT,
        COUNT,
        MAX,
        MIN,
        AVG,
        SUM
    }
}
